import {describe, it, expect} from 'vitest';

// Placeholder test — proves the vitest wiring runs in CI. Real tests land later.
describe('workspace smoke', () => {
  it('executes the test suite', () => {
    expect(1 + 1).toBe(2);
  });
});
