/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited.
// It is committed so that `tsc --noEmit` resolves Next's ambient types in CI
// without first running `next build`.
